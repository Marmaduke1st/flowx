## What is FlowX 

FlowX is an automated workflow, combiming Git, GitFlow and GitLab

## How this will work
`git init` & `git flow init` plus `glab repo create` will be replaced by `flowx init [path] [flags]`

